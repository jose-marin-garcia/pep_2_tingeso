server:
  port: 8091
spring:
  datasource:
    url: jdbc:nysql://localhost:3306/vehicles-db
    username: root
    password: mysql2024$
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka